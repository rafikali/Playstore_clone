import 'package:flutter/material.dart';


class Editorchoice extends StatelessWidget {
  static const String routeName = '/editorchoice';
  const Editorchoice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
